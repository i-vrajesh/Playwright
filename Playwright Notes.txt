-----------------------------------------------
> Playwright: https://playwright.dev/docs/intro
-----------------------------------------------
> Reliable End-to-End Testing - Autowait capability (Time depends on global wait)
FOr Selenium need to handle this explicitly.
> Cross Browser compatability
> Multiplatform Support - Windows, MacOs, Linux
> Multi lingual Flexibility - JS, TS, Python, Java & C#(.Net)
Cypress only support JS & TS ad it is JS based testing FW.

---------------------------------
> Playwright's Advanced Features:
---------------------------------
> Tracking & debugging: Provides automatic Screenshots, video recordings & comprehensive logging without explicitly mentioning.
> Network Interception: Mocking and Intercepting. 
> Browser Context Managenent: If logged in normal browser, that session's cookies are handled automatically to launch application same in incognito 
> Codegen Tool: Generate tests by recording actions.

----------
> Node.js:
----------
An open-source, cross-platform, back-end JavaScript runtime Environment that runs on the V8 engine and executes JS code outside the browser.

----------
> VS Code:
----------
Best IDE developed by MicroSoft famously used for JS related projects
/* node package manager will initializes a new Node.js project. 
Its primary function is to create a package.json file in the current directory, which serves as a manifest for the project. 
This file contains metadata about the project and manages its dependencies */

> npm init 					//creates package.json file
> npm install @playwright/test 			//downloads and add playwright version in package.json
> npx playwright install 			//downloads all browsers for playwright testing
> npx playwright test –headed 			//To run in headed mode
> npx playwright test --headed --browser=firefox //To run in specific browser
> npx playwright test --headed --browser=all 	//To run all browsers
> npx playwright test tests/example.spec.ts 	//To run from specific file
> npx playwright test --ui			//opens Ui test runner for entore tests folder

--------------------------
const {test} = require('@playwright/test');
test('Test Title',async ()=> {
//each line await added to wait completing previous line
//if we write await in method/function, then it should be changed to async function - async & await comes as combination
//await activated only if we mark function as async
//()=> anonymous function(Fat operator)
}

----------------------------------
> Browser Context and PageFixtures
-----------------------------------
//Below both are same, playwrihght automatically manage page if we won't specify context options

test("Browser Invoke1", async ({browser})=>{ //here {browser} is a playwright global fixture (nothing global variable available across whole project)
    const context = browser.newContext();
    const page = context.newPage();
    await page.goto("http://www.rahulshettyacademy.com/loginpagePractise/");
}); 

test("Browser Invoke2", async ({page})=>{  
    await page.goto("http://www.rahulshettyacademy.com/loginpagePractise/");
});

------------
test.only
-----------
test.only("Browser Invoke2", async ({page})=>{  
//when we want to run only 1 TC deom entire file, then test.only partivular test will execute

---------------------
playwright.config.js:
---------------------
// @ts-check
const { defineConfig, devices, chromium } = require('@playwright/test');

module.exports = defineConfig({ //exporting defineConfig variable (JS object) across all the files in project 
  testDir: './tests',//directing to tests folder in directory level like glue
  reporter : 'html',//default reporter, we can add allure also
  timeout: 40*1000, //explicitly overriding 30s default timeout
  expect: {
    timeout: 5000,//timeout for assertions overriding 30s default timeout
  },
  use: {
    browserName: 'chromium',
    headless: false,
    screenshot:'on', //for every action
    trace: 'on', //logging 
  },
});

------------------
> async and await:
------------------
Playwright is built with an asynchronous architecture from the ground up, natively supporting JavaScript's async/await keywords.
Unlike a synchronous script that executes one line after the other instantaneously, Playwright's APIs return Promises. 
The await keyword explicitly tells the test script to pause and wait for that Promise (PENDING, FULFILLED/ RESOLVED, REJECTED) to resolve before proceeding to the next step.
interacting with a web browser involves waiting for commands to be sent and for the browser to respond.

Asynchronous actions	: Browser operations like page.goto(), page.click(), or page.fill() take time to complete. 
			  They return a Promise, and without await, the script would move on before the action is finished, causing flaky tests.
Sequential execution	: Using await pauses your script until the previous browser action is confirmed as complete. 
			  This ensures actions execute in the correct order, making tests reliable and predictable.
Clearer code		: async/await makes asynchronous code read like synchronous code, which greatly improves readability and maintainability. 

---------
> Waits:
---------
    await page.waitForLoadState('networkidle'); 	// wait till all api calls completed in backed
    await page.waitForLoadState('domcontentloaded'); 	// wait till dom content loaded
    await page.waitForLoadState('load');		// to pause execution until the entire web page has finished loading all its resources.
    cardTitles.waitFor();				// simply wait for that locator
    console.log(await cardTitles.allTextContents());	// allTextContents method doesn't have automatic wait, bcoz we are specifically targetting to one element

-----------
> Locators:
-----------
If ID is present 				- css -> tagname#id (or) #id
If class is present 				- css -> tagname.class (or) .class
Write css based on any attribute 		- css -> [attribute='value']
Write css with traversing from Parent to child 	- css -> parenttagname >> childtagname
If needs to write locator based on text 	- css -> text=''


--------------------
> type() and fill():
---------------------
    await username.type("admin");	//type method deprecated
    await username.fill("rahulshetty");	//like sendkeys in selenium

-------------------------------------
> first(), last(), nth(): Like Index
-------------------------------------
    console.log(await cardTitles.first().textContent());
    console.log(await cardTitles.last().click();
    console.log(await cardTitles.nth(1).textContent());

-----------------------
> Playwright Inspector:
-----------------------
    await page.pause(); 		// will launch playwright inspector like debugging

-------------------------
> Finding locator with text
--------------------------
await page.locator("text= Add To Cart").click(); //applicable to whole page
await page.locator("h3:has-text('Zara Coat 4')").click(); //applicable to whole page with specific tag (called as pseudo class)

----------------------------------------------
> Auto Suggestive dropdowns/ Dynamic dropdowns
----------------------------------------------
pressSequentially : similar to type or fill

   await page.locator("[placeholder*='Country']").pressSequentially("ind");
   await page.locator("[placeholder*='Country']").pressSequentially("ind", { delay: 150 });
   const dropdown = page.locator(".ta-results");
    await dropdown.waitFor();
    const optionsCount = dropdown.locator("button").count();
    for(let i=0; i<optionsCount;i++){
        const text = await dropdown.locator("button").nth(i).textContent();
        if(text === " India"){
            await dropdown.locator("button").nth(i).click();
            break;
        }
    }

Here, a delay of 150 milliseconds is introduced between each key press.
That means it enters  i → (delay 150 ms) → enters n → (delay 150 ms) → enters d

------------------
> Normal dropdown:
------------------
await page.getByLabel("Gender").selectOption("Female");//selectOption only works if element located in <select> tag

------------
> Locators:
------------
In general day to day life project framework most common suggested locator is css or xpath, below are extra benefitial locator methods

page.getByRole() 	- to locate by explicit and implicit accessibility attributes.
page.getByText() 	- to locate by text content.
page.getByLabel() 	- to locate a form control by associated label's text.
page.getByPlaceholder() - to locate an input by placeholder.
page.getByAltText() 	- to locate an element, usually image, by its text alternative.
page.getByTitle() 	- to locate an element by its title attribute.
page.getByTestId() 	- to locate an element based on its data-testid attribute (other attributes can be configured).
oage.locator()		- default for css and xpath

<input class="btn btn-success" type="submit" value="Submit">//it need not be <button> tag, btn class is enough to identify
await page.getByRole("button",{name:'Submit'}).click();

<any tag>Success</any tag> 
await page.getByText("Success").isVisible();//will search in entire page like contains(text(),'')

<label class="form">Check me out if you Love IceCreams!</label>
await page.getByLabel("Check me out if you Love IceCreams!").check();

<input class="form" id="password" placeholder="Password" type="password">
await page.getByPlaceholder("Password").fill("admin123");

<label for="exampleFormControl">Gender</label>
await page.locator("app-card").filter({hasText:'Gender', has:'', hasNot:'', hasNotText:'Email'}).getByRole("button",{name:"Add to Cart"}).click(); //4 filters

-------------
> Calenders:
--------------


-------------------
> Back and Forward
-------------------
    await page.goBack();
    await page.goForward();

-----------------
> Alert & Popups
-----------------
In playwright we call as dialouges
page.on()//on method waits to listen for events to occur

    await page.on('dialog',dialoug => dialoug.accept());
    await page.on('dialog',dialoug => dialoug.dismiss());

--------
> Hover
--------
await page.locator("#mousehover").hover();

---------
> Frames
---------

    const framesPage = page.frameLocator("#courses-iframe"); //switching from page to framesPage
    await framesPage.locator("li a[href*='lifetime-access']:visible").click();//performing using framesPage

--------------
> API Testing
---------------
const {request} = require('@playwright/test'); //request need to import for api testing

-------------
> Screenshot
---------------

await page.locator("#id").screenshot({path:locatorScreenshot.png}); //locator level
await page.screenshot({path:screenshot.png}); //page level

//visual testing.. matching exact locator visual representation to older img
expect(await page.screenshot()).toMatchSnapshot("landing.png");//comparing with old png


-----------
> ExcelJS
----------
add this plugin in package.json to handle with xcel and json

  const downloadPromise = page.waitForEvent('download');
  await page.getByRole('button',{name:'Download'}).click();
  await downloadPromise;//wait till download is fullfilled/resolved

  const ExcelJs =require('exceljs');
  const workbook = new ExcelJs.Workbook();
  await workbook.xlsx.readFile(filePath);
  const worksheet = workbook.getWorksheet('Sheet1');


---------------------
> Data driven Testing
---------------------

Use JSON in different folder as data file
{
    "userName":"admin",
    "password":"pass123"
}

const data = JSON.parse(JSON.stringify(require("../utils/data.json")));
//converting to stringify to remove unnecessary upcoming issues related to UTF-8 encoding amd later we convert to js objects using JSON.parse to convert from JSON to js object

Now in methods we can use data.userName, data.password

-----------------------------------------------
> Parameterize tests with different data sets
-----------------------------------------------
//Repeat test with multiple testdatas

Like earlier usage
implement multiple data sets inside JSON

[
  {
    "userName": "admin1",
    "password": "pass123"
  },
  {
    "userName": "admin2",
    "password": "pass1234"
  }
]

//After calling that file and storing it in data variable apply for loop outside test

const data = JSON.parse(JSON.stringify(require("../utils/data.json")));

for(const d of data){
test(`TestName User - ${d.userName}`,async ({page})=>{
............
});
} 
//this test will run for 2 times as array have 2 sets of data

--------------
> AI LLM
--------------
In new playwright inspector when a test failed, it will automatically provide a "copy prompt" button in console which is useful to give a prompt to AI model like copilot  or chatgpt to give quick resolution


--------------------------------------------
> To run from different playwright.config1.js
--------------------------------------------

For Ex: you have playwright.config.js [chrome and headed] and playwright.config1.js [webkit, headless]

Then in terminal > npx playwright test tests/UiTesting.spec.js --config playwright.config1.js  //by default it will consider playwright.config.js

Instead of maintaining 2 config files for 2 types of settings
create an array of projects as below in playwright.config.js

  projects: [
    {
      name: 'chromium',
      use: { 
        browserName: 'chromium', //firefox, webkit
        headless: false, //headmode or headless
        screenshot:'only-on-failure', //only when error
        trace: 'on', //logging like log4j
        viewport: {width:720,height:720},
      },
    },
    {
      name: 'firefox',
      use: { 
        ...devices['Desktop Firefox'] //['iPhone 11'] can mention available devices
      },
    },
    {
      name: 'webkit',
      use: { 
        browserName: 'webkit', 
        headless: false, //headmode or headless
        screenshot:'off', //for every action
        trace: 'on', //logging like log4j
      },
    },
  ],


After arranging projects in config file
Then in terminal > npx playwright test tests/UiTesting.spec.js --project=webkit //if we don't mention any project specifically, then all 3 defined will execute in 3 workers

-------------------
> SSL Certificate 
-------------------
For few websites starts with http:// instead of https://
Not Secure/ Your Connection is not private errors

Playwright automatically ignores by moving to advanced and accepts it using ignoreHttpErrors:true

    {
      name: 'firefox',
      use: { 
        ...devices['Desktop Firefox']
        ignoreHTTPSErrors: true, //bypass SSL Certificate Errors
      },
    },

----------------------
> Geo Location popups
----------------------

For few sites it asks geo location permissons.. playwright automatically handles that
    {
      name: 'firefox',
      use: { 
        ...devices['Desktop Firefox'],
        ignoreHTTPSErrors: true,
        permissions:['geolocation'],// allows gerlocation to the site
      },
    },

------------------
> Video Recordings
------------------

'off' - Do not record video.
'on' - Record video for each test.
'retain-on-failure' - Record video for each test, but remove all videos from successful test runs.
'on-first-retry' - Record video only when retrying a test for the first time.


import { defineConfig } from '@playwright/test';
export default defineConfig({
  use: {
    video: {
      mode: 'on-first-retry',
      size: { width: 640, height: 480 }
    }
  },
});

--------------
> Retry Tests
--------------

Add retries:1 in config level not in project or use array level bcoz it applicable to whole configuration

------------------------------
> Series & Parallel Execution
------------------------------

By default all spec.js files will run in Parallel & all tests in each spec.js file will run in Series 


To run tests in spec.js parallel add below in that spec.js file

test.describe.configure({mode:'parallel'});

test.describe.configure({mode:'serial'}); //if we explicitly mention serial then tests inside that spec file will be skipped if any one TC is failed kind of dependancy


----------------
> Tagging Tests
----------------

Writing @<Tag> inside testName and grep(regular expression) to filter only specified tags for execution


test(' @Web Client APp Login ', async ({page})=> {
....
}),

In terminal > npx playwright test --grep @Web
 
--------------
> HTML Reports
--------------

Allure Reports : npm i -D @playwright/test allure-playwright
i for installation
-D for parameter

To generate Allure reports: 
npm playwright test <path/tag> --reporter=line,allure-playwright 
//line will generate a text format report which will be utilised by allure to create allure reports

//after above command execution afolder with allure-results will be cfreated with all required json and img contents
allure generate ./allure-results --clean 

// after executing above command a new folder will be created naming allure-report where main report will be generated 
allure open ./allure-report


-------------------------
> Creating custom scripts
-------------------------

In package.json we can define scripts like below and can execute through CLI

 "scripts": {
    "test": "npx playwright test tests/ApiTesting.spec.js --headed",
    "regression": "npx playwright test",
    "WebTests": "npx playwright test --grep @Web",
    "ApiTests": "npx playwright test --grep @Api",
    "safari": "npx playwright test --project=safari"
  },

Then in terminal: 
npm run WebTests //this will directly point to package.json and search in scripts and execute assigned to it

----------
> Jenkins
----------

Jenkins Integration

New Item > Give name as Playwright > Select Freestyle project > Click OK
Peoject Configutaion page will opens
Gerenal configuration settings > Git/ Local > local path directory
Select command prompt shell/bash
give command as npm run regression


---------------------------
> Playwright from JS to TS
---------------------------

TS is superset of JS

Javascript - Dynamic typing (automatically identify str or int)
TypeSCript - Static typing ( explicitly need to mention data type)

to run demo.js -> node demo.js
to run demo.ts -> tsc demo.ts -> creates demo.js.. then -> node demo.js

Playwright supports TypeScript out of the box. You just write tests in TypeScript, and Playwright will read them, transform to JavaScript and run.

//If we not mention strong typing, then .ts file will throw error at compilation but still compiles to .js and runs the .js file in .js format.

npm install --save-dev typescript //installs typescript

> Class - Exports

in JS:
------

const {LoginPage} = require('./LoginPage');

class LoginPage{

}
module.exports = {LoginPage} //need to export at last

in TS:
------
import {LoginPage} from './LoginPage';

export class LoginPage{ //export class level
}

--------------
> Cucumber JS
--------------

npm install @cucumber/cucumber --save-dev

After installing cucumber and writing cucumber feature file 
To generate cucumber skeleton step defenitions, dry run the feature file

in terminal:
npx cucumber-js 
//will search folder in node-modules... inside that code written ro check features folder in entire directory and executes/dryryns all feature files and generates skeleton step defs..

to run specific feature file
npx cucumber-js feature/login.feature --exit

> World Constuctor - Cucumber Playwright:
------------------------------------------

using this.

this.loginPageObj = new LoginPage(page) //this. will allow to use the same object (means like global variable) in any other method as long as that methods stays in the same test scenario

> Hooks in Cucumber:
---------------------
const {BeforeAll, BeforeStep, Before, After, AfterStep, AfterAll} = require('@cucumber/cucumber');

BeforeAll(function () {
  // perform some shared setup for all scenarios 
});

BeforeAll(function(){
  //performs before for each scenario
});

BeforeScenario(function(){
  //performs before every step
});

> Tags:
--------
to run
npx cucumber-js --tags "@Regression" --exit 
//runs all tags which have tag @Regression
// --exit Ensures the process quits immediately after the tests are finished, regardless of any lingering background asynchronous tasks.

Tags can also used in hooks

Before({tags:"@Regression" and "Sanity"}, function(){
//will execute before scenarios only having both tags
}) 


> Parameterization:
--------------------
Scenario Outline:
	Given..When..Then..
Examples:
	| UserName | Password |
	| admin1   | pass123  |
	| admin2   | pass124  |

> Parallel Execution:
---------------------
Cucumber can only runs Test Scenarios in parallel - Not features

in Terminal:
npx cucumber-js features/Login.feature --parallel 2 --exit
//here 2 means like 2 threads.. 2 scenarios will run paralely at a time

> HTMl Reports:
----------------
in Terminal:
npx cucumber-js features/Login.feature --exit --format html:cucumber-report.html

> Rerun Failed:
---------------
In Configuraion file:
{retry:1}

In Terminal:
npx cucumner-js features/Login.feature --retry 1 --exit

In package.json in scripts:

"scripts":{
  "Cucumber":"npx cucumner-js features/Login.feature --retry 1 --exit"
}

npm run Cucumber //to run in CLI

//In cucumberhtml report it will not show flaky like playwright default report of it rerun 


-------------------------------------------------------------------------
> E2E Playwright Devops Soluton using Azure Cloud Parallel hosting & CICD 
-------------------------------------------------------------------------
... skipped .. need to check after

---------
> GenAI
---------

MCP - Model Contect Protocal
we need to train or model that if we give snippet like below

navigate to "url"
login ewith admin , pass123
check cart and logout

It should able to undestand natural english language and selects relevant cod and methods to write code accordingly and execute automatically

Change copilot to agent mode in VS code
In servers add @playwright/mcp

Give prompt and follow accordingly in copilot chat section... it will generate code

---------
> FAQ's
---------

00:21 - Can a JavaScript object hold a function as a property? Explain with an example.
Ans: Yes
cost person = {
	name : "John",
	age : 30,
	greet : function(){
		   	console.log("Hello");
		}
	}

03:28 - What are anonymous functions in JavaScript? Define their syntax and implementation
Ans: Function doesnot have names... collect output of anonymous function in a variable and call that variable

07:22 - What is the difference between var, const, and let? Explain with an example.
Ans: var - function scoped or globally scoped and can be re-declared and updated.. without declaration by default they are var variables
let - block scoped and can be updated but not re-declared within the same scope
const - is block scoped and cannot be updated or re-declared

16:26 - Where are the push, pop, slice, shift, and unshift methods used when accessing array elements?
Ans: const fruits = ["apple","mango"];
//access
console.log(fruits[1]);//mango
//add an element to the end of array
fruits.push("grape");
//remove the last element from array
fruits.pop();
//add element to the beginning of array
fruits.unshift("banana");
//remove first element from array
fruits.shift();
//find index of element
fruits.indexOf("banana");
//remove an element by index
fruits.splice(1,3); //index 0 removed and started from index 1
//iteration
fruits.forEach((fruits,index)=>{
    console.log(`${index} : ${fruits}`);
})
// 0 : banana
// 1 : apple
// 2 : mango
// 3 : grape


23:19 - Is JavaScript Asynchronous? Prove with an example.
Ans: 
console.log("1");
setTimeout(function(){
    console.log("2");
},2000); //waits 2 seconds but executed 3rd w=one without waiting for it
console.log("3");

27:53 - What are callback functions in JavaScript?
Ans: A callback function  that is passed as an argument to another function and is executed after some operationhas been completed. Callbacks are often used for asynchronous operations.

function fetchData(callback){
    const data = "Sample data";
	callback(data);
}
function processData(data){
	console.log("Processing: "+data);
}
fetchData(processData);

38:09 - What are promises in JavaScript? Explain the difference between callback functions and promises with an example.
Ans: Promise - resolve, pending, reject

function fetchData(callback){
    return new Promise((resolve)=>{    
        const data = "Sample data";
        callback(data);
        resolve(data);
    });
}
function processData(data){
	console.log("Processing: "+data);
}
fetchData().then(function(data)){
    processData();
}
//OR below simple await
const data = await fetchData();
processData();

49:15 - Create an inheritance relationship between a parent and child class. Invoke the parent constructor from the child class. Create main.js to call parent class methods from a child class object.
Ans: using extenfds and super keyword

55:21 - From the above code, explain how the super and this keywords help achieve the solution.
Ans: super can only used in child class which extends its parent class
this keyword can use in any which where this.variable will assessible entire file

01:00:05 - What is the difference between == and ===?
Ans: ==compares value and === copares value and type
console.log(5=='5'); //true
console.log(5==='5'); //false

01:03:32 - What is the difference between null and undefined in JavaScript?
Ans: 
let a = null
console.log(a) //null
console.log(typeof a) //object - null is considered as an object
let b
console.log(b) //undefined
console.log(typeof b) //undefined is not even confirmed as object.. that type also undefined

01:06:36 - A classic programming interview question that involves using array methods (filter, map, reduce), and JavaScript objects.
Ans:
const students = [{name:"alice",score:45},
{name:"john",score:66},
{name:"mark",score:34},]
//filtering passed students
const passedStudents = students.filter(students=>students.score>=35);
console.log(passedStudents)
//converting passed students names to upper case
console.log(passedStudents.map(passedStudents=>passedStudents.name.toUpperCase()))
//total marks of passed students
const totalScore = passedStudents.reduce(function(acc,student){
    acc = acc+student.score;
    return acc;
},0)
console.log(totalScore);

